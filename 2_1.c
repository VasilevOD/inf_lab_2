#include<stdio.h>

int main(void) {
    int stroka;
    while ((stroka=getchar()) != '\n') {
        if(c != ' ' && stroka != '\t'){
            putchar(stroka);
        }
    }
    return 0;
    
}
